# 🚀 Настройка Telegram Webhook

## 🚨 **ВАЖНО: Без webhook кнопки не работают!**

### **Проблема:**
- ❌ Кнопки "Подтвердить"/"Отклонить" не работают
- ❌ Статус заказа не меняется при нажатии
- ❌ Webhook не настроен на Render

### **Решение:**

## 1️⃣ **Настройка переменных окружения**

Убедитесь что в Render добавлены:
```
TELEGRAM_BOT_TOKEN=ваш_бот_токен
TELEGRAM_ADMIN_CHAT_ID=ваш_chat_id
```

## 2️⃣ **Установка Webhook**

### **Способ 1: Через скрипт (рекомендуется)**

```bash
# На Render (в Shell):
python setup_webhook.py
```

### **Способ 2: Вручную через API**

```bash
# URL для установки webhook:
https://api.telegram.org/botВАШ_ТОКЕН/setWebhook?url=https://stationery-0lp6.onrender.com/telegram/webhook/
```

### **Способ 3: Через @BotFather**

1. Найдите **@BotFather** в Telegram
2. Отправьте: `/mybots`
3. Выберите вашего бота
4. Нажмите **"Bot Settings"** → **"Webhook"**
5. Введите URL: `https://stationery-0lp6.onrender.com/telegram/webhook/`

## 3️⃣ **Проверка Webhook**

### **Проверить статус:**
```bash
python setup_webhook.py
```

### **Или через API:**
```bash
https://api.telegram.org/botВАШ_ТОКЕН/getWebhookInfo
```

### **Правильный ответ:**
```json
{
  "ok": true,
  "result": {
    "url": "https://stationery-0lp6.onrender.com/telegram/webhook/",
    "has_custom_certificate": false,
    "pending_update_count": 0,
    "last_error_date": null,
    "last_error_message": null
  }
}
```

## 4️⃣ **Тестирование**

### **1. Проверьте логи Render:**
```
Получены данные от Telegram: {"callback_query": {...}}
Получен callback_query: {...}
Обработка callback data: confirm_payment_8
Подтверждение оплаты заказа #8
```

### **2. Проверьте в Telegram:**
1. Создайте заказ
2. Нажмите "Я оплатил"
3. В Telegram нажмите "Подтвердить"
4. Должно появиться: "✅ Оплата подтверждена администратором."

### **3. Проверьте статус заказа:**
- В базе данных: `paid=True, status='confirmed'`
- На странице: "✅ Оплата подтверждена!"

## 5️⃣ **Troubleshooting**

### **Если webhook не работает:**

#### **Проверьте URL:**
- Правильный ли домен: `stationery-0lp6.onrender.com`
- Есть ли `/telegram/webhook/` в конце
- Доступен ли URL (откройте в браузере)

#### **Проверьте токен:**
- Правильный ли `TELEGRAM_BOT_TOKEN`
- Не истек ли срок действия токена

#### **Проверьте SSL:**
- Render использует HTTPS - webhook должен быть HTTPS
- Сертификат должен быть валидным

### **Если кнопки не отвечают:**

#### **Проверьте логи:**
```
Получены данные от Telegram: {"message": {...}}  # Не callback_query
Получен другой тип данных: ['message']  # Нажатие на кнопки не приходит
```

#### **Решение:**
1. Удалите webhook: `/deleteWebhook`
2. Установите заново: `/setWebhook`
3. Проверьте `allowed_updates: ['callback_query']`

---

## 🎯 **Результат после настройки:**

- ✅ **Кнопки работают:** Подтверждение/отклонение
- ✅ **Статус меняется:** paid=True, status='confirmed'
- ✅ **Пользователь видит:** "✅ Оплата подтверждена!"
- ✅ **Перенаправление:** На страницу заказа

**⚠️ ВАЖНО:** Webhook нужно настраивать **ОДИН РАЗ** после деплоя на Render!
